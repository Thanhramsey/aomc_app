import 'package:flutter/material.dart';

class ActionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Action")),
      body: Center(child: Text("Nội dung Action")),
    );
  }
}
